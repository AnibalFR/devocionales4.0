# Server
NODE_ENV=production
PORT=4000
LOG_LEVEL=info

# Database (IMPORTANTE: Cambiar con la contraseña generada por el script de setup)
DATABASE_URL=postgresql://devocionales:TU_PASSWORD_AQUI@localhost:5432/devocionales_prod

# JWT (IMPORTANTE: Generar con: openssl rand -base64 64)
JWT_SECRET=CAMBIAR_POR_SECRET_GENERADO
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=https://tudominio.com

# GraphQL
GRAPHQL_INTROSPECTION=false
GRAPHQL_PLAYGROUND=false
GRAPHQL_DEBUG=false
